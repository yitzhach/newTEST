{
  "name": "show-ledger-intel",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply show-ledger-intel --local",
    "migrate:remote": "wrangler d1 migrations apply show-ledger-intel --remote",
    "test": "bash test/run.sh"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
