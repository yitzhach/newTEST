# Working agreement — read this first, every session

1. **Be brief.** Short answers. No preamble, no recaps, no bullet lists of
   options I didn't ask for. Save tokens.
2. **Push for fresh chats.** When a chat gets long or we finish a chunk of
   work, say so and recommend starting a new one.
3. **Before ending a chat:** save all work to files, commit, and update
   `PROJECT_STATE.md` so the next chat can pick up cold. Then end your last
   message with a copy-paste-ready prompt in a fenced block that starts the
   next phase in a fresh chat.
4. **Read `PROJECT_STATE.md`** at the start of every session — it's the
   handoff doc.
5. Branch for this work: `claude/art-show-tracker-smoke-18dbj6`.
   It is `claude/artist-show-tracker-5pavpl` plus the Phase 6 and smoke-harness
   commits — a strict fast-forward, so 5pavpl is redundant and can be deleted.
   The tracker itself ships from `main` (GitHub Pages).
